# 03 NEXT ACTIONS AND OPEN QUESTIONS（家事アプリ・次にやること／仮説／未決事項）

> **正本。** Sprintごとに更新する。終わったものは消し、01・02に反映する。HYP番号は旧HYPOTHESES.md（`archive/`）と対応。
> 最終更新：2026-09-25

## 1. 次にやること（優先順）

| # | 内容 | 優先度 | 状態 | メモ |
|---|---|---|---|---|
| N1 | **実際の夕飯での試用（Dogfood）を始める** | 高 | 未着手 | 試用を始めてよい状態。記録はまだ0件 |
| N2 | アプリを使わずに決めた日の時間を、アプリの外で2〜3回メモする（比べる基準） | 高 | 未着手 | 「触る時間が減ったか」を判定するため |
| N3 | **前回使わなかった食材の自動引き継ぎ**（次Sprintの最有力候補） | 高 | 保留 | 「負荷の消滅」の効果が最も大きい。一方で、使い切った食材が残って消す手間が増えるおそれ。記録（`removed_chip_count`）が数日分たまってから判定 |
| N4 | 成功指標の合格ラインを決める | 中 | 未着手 | 計測は実装済み。例：献立の採用率、決めるまでの秒数 |
| N5 | iPhone Safariで、変換確定のEnterで誤登録されないか実機確認 | 中 | 未着手 | 自動テストでは確認済み |

## 2. 仮説（すべて未検証）

| # | 仮説 | 検証の手がかり | 状態 |
|---|---|---|---|
| HYP-001 | 在庫を入力しなくてよいAIの方が、高性能なレシピAIより価値がある | PoC-B（写真・レシート） | 保留 |
| HYP-002 | 献立候補は1案の方が迷わない（時短モード限定の可能性） | 1案＋「別案」と3案の比較。**モード未選択時は「モードをまたぐ3案」（`mode:'mixed'`）なので、比べるときはモード選択の有無も条件にする** | 保留（操作が落ち着いてから） |
| HYP-003 | 👍😐👎だけの簡単な評価で家庭の好みを学習でき、継続利用につながる | 学習ループは実装済み。未評価・「作らなかった」が多いと材料が薄くなる。補正は同じレシピの直近1件だけを見ている | 検討中（試用で確認） |
| HYP-004 | 家事全体の拘束時間（Household Load）で比べると差別化できる | 今は調理時間と「買い物不要」だけ。洗い物・下処理の量は42件に手でタグ付けが必要 | 保留（次の次） |
| HYP-005 | 人が求めているのは在庫管理アプリではなく、在庫管理をしなくて済む仕組み | PoC-B | 保留 |

## 3. 未決事項・オーナー判断待ち

| # | 論点 | 状態 |
|---|---|---|
| Q1 | アプリの正式名称 | オーナー判断待ち（今は決めない） |
| Q2 | PoC-B（写真・在庫）へ進むタイミング | PoC-Aを実生活で使って便利と確認できてから（オーナー判断） |
| Q3 | N3を次のSprintに入れるか | 推奨は「計測が数日分たまってから」 |

## 4. 保留・作らないもの（再検討の条件つき）

| 対象 | 扱い | 再検討の条件 |
|---|---|---|
| 洗い物の量のタグ | 保留（次の次） | 「買い物不要」の価値を見てから |
| 栄養（3色タグ・同点時の栄養並べ替え） | 保留 | 数週間分の記録を書き出して偏りが分かってから |
| 「わが家の定番リスト」への切り替え | 保留 | 学習で👍の料理が自然に上がるかを見てから |
| 1案／3案の切り替え・「別案」ボタン | 保留 | HYP-002。操作の変更が落ち着いてから |
| 家族一人ひとりの評価・完成写真 | 作らない | まず世帯全体の👍😐👎で足りるか |
| 買い物リスト・レシピ手順画面 | 作らない | 標準のリマインダー・共有メモ・既存サイトで足りる |
| 栄養計算・カロリー・人ごとの摂取量 | 作らない | 分量の入力が必要になる（G2・G7） |
| 本格データモデル（Household・Consent・InventoryLot等） | 作らない（PoC中） | localStorageで動くPoCには過剰。設計案は`archive/DATA_MODEL.md` |

## 5. PoC-Bに進むときに必須になること

- 画像・音声は確認後7日以内、遅くとも30日で削除（DEC-006）
- 写真の位置情報（EXIF）の除去、レシートの決済情報のマスク、家の中・学校のプリント・会員番号などの写り込みを減らす
- 外部AIへ送る前の確認（送る内容・保存期間・学習利用・再委託・削除方法）と別の同意

## 6. 既知の限界

- 除外食材は「主な食材」でしか判定できない。調味料・加工品に含まれるものは判定できない（警告と注意書きで補う）。他の家庭に展開する前に、原材料レベルの判定と受入テスト（卵を除外したら、卵を含む料理・代替品を通さない）が必要。
- 読み替えの粒度が粗い（例：「そば粉」→麺で麺料理がまとめて外れる。警告は出る）。
- レシピ42件・辞書が小さく、狙った料理が出ないことがある（検索リンクで補う）。
- 記録はこの端末のブラウザだけ。家族の別端末とは共有できず、ブラウザのデータを消すと記録も消える（書き出しで退避できる）。

## 7. 直近のレビュー

- [reviews/2026-09-25_home-review.md](reviews/2026-09-25_home-review.md)（7観点。①〜⑤をSprint1として実装済み。6章の論点はDEC-009・DEC-010で解決）
