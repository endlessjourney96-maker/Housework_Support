# 02 DECISIONS AND GUARDRAILS（家事アプリ・確定した判断とガードレール）

> **正本。今有効な判断だけを書く。** 判断が変わったら**書き換え**、古い判断は `archive/` に移す。DEC番号はレビュー記録と対応しているので変えない（旧DECISIONS.mdは `archive/`）。
> 最終更新：2026-09-25

## A. ガードレール（破ってはいけない線）

| # | ガードレール | 理由 |
|---|---|---|
| G1 | **「負荷の移動」ではなく「負荷の消滅」を狙う。** 新しい機能・入力は「家の人が考える・入力する・動く量が本当に減るか」で判断する。改善より「その操作を無くせないか」を先に問う | 入力を別の入力に置き換えても、家事は減らない |
| G2 | **AIのために人がデータを入力しない。** 記録したら必ず次の提案に返す | 返ってこない記録は、ただの追加の家事 |
| G3 | **AI（推測）は事実を勝手に確定しない。** 人が確認した記録をAIが上書きしない。「不明」を0や0分に変換しない | 家庭の情報を勝手に事実にしない（TimeMagic・教育アプリと共通） |
| G4 | **安全の条件（アレルギー・苦手）は推薦の順位より必ず優先する。「安全です」とは言わない。** 不明な原材料・読み替えは警告し、人の確認に回す | 材料表は主な食材だけで、完全には判定できない |
| G5 | **家庭のデータは本人の手元に置く。** 目的ごとに同意を分ける。外部AIは、送る内容・保存期間・学習利用・削除方法を確認してから別に同意 | 家庭データを預かる責任 |
| G6 | **アプリを触る時間が短いほど成功。** 利用時間の増加を成功にしない | 目的は負担を減らすこと |
| G7 | **健康・栄養・節約などを全部最初のPoCに入れない。** 一度に変える要素は1つ | 何が効いたか分からなくなる |
| G8 | **既存サービスで足りるものは作らない**（作り方・買い物メモ・レシピDBの拡充） | 家事の負担を開発側の保守に移すだけになる（Native First） |
| G9 | **公開リポジトリに家族の個人情報・生活パターン・アレルギー情報を書かない。** 書き出したJSONやExcelはコミットしない | リポジトリはPUBLIC |

## B. 確定した判断（DEC番号）

| # | 判断 | 理由 |
|---|---|---|
| DEC-001 | 教育アプリとは完全に別プロジェクト | 目的・利用者・安全要件が違う |
| DEC-002 | Evidence→Candidate→Confirm→Learningの流れを採用する | G3 |
| DEC-003 | 入口は「食事・献立」 | 毎日起きる・迷う負担が大きい・効果が見えやすい・家庭で試しやすい |
| DEC-004 | モードは時短と料理を楽しむ系から始め、最初から増やしすぎない（実装は⚡最速／🍳普通／🎨料理したい）。前回のモードを初期値にしない | 疲れた日に「料理したい」のまま進まないため |
| DEC-005 | 評価軸は食材の最適化ではなく、家事全体の負担（Household Load＝調理＋下処理＋洗い物＋買い物） | おいしさと家庭にとっての良さは別 |
| DEC-006 | 実データを入れる前に、同意・保存期間・削除の最低限要件を満たす（目的の分離／画像・音声は確認後7日以内・遅くとも30日で削除／構造化記録は90日で見直し／世帯単位の書き出し・削除／外部AIは確認してから） | 家庭データを扱う前提条件 |
| DEC-007 | PoC-Aは在庫認識を含めない（冷蔵庫写真・レシート・在庫推定はPoC-B）。範囲は食材入力→モード→候補→選ぶ→評価 | 確かめたいのは在庫認識の精度ではなく、献立を考える負担が減るか |
| DEC-008 | レシピは42件で固定し、DB・辞書を都度拡充する運用はしない。足りない分は検索リンクで補う | G8 |
| DEC-009 | DEC-006を実装：初回の同意画面（サービス利用＝必須／研究利用＝任意）、90日を過ぎた記録は自動削除せず「書き出して消す／残す」を聞く、書き出し・全削除・同意の撤回。写真・音声は扱わないので7日/30日ルールはPoC-Bで導入 | オーナー判断（2026-09-25）「同意と保存期間の表示まで先に入れて試す」 |
| DEC-010 | 除外食材（アレルギー・苦手）を、同意画面（事前）と設定欄（事後）の両方で登録できる。該当レシピは候補に入れない。辞書外・読み替えは警告 | オーナー要望（2026-09-25）。オーナー家庭は現状該当なし、今後のため |
| DEC-011 | 記録は「選んだ時点」で自動保存し、評価は1タップ・任意。「また作りたい？」・保存ボタン・完了のポップアップは持たない | G1（食後の4操作を1操作に） |
| DEC-012 | 学習はAIを使わず、普通のルールで小さく補正する（👍+0.5／👎−2／3日以内に作った−1）。直近に作った料理は外さず下げる | G2・G3。外すとモード内の候補が足りなくなる |
| DEC-013 | モード未選択でも候補を出す（各モードから1案ずつ） | 選ぶ操作を消す（G1） |
| DEC-014 | 同点時は「買い物不要」（買い足しが少ない方）を先にする。栄養による並べ替えは入れない | DEC-005の買い物部分を先行。G7 |
| DEC-015 | 写真・レシートの画像認識と外部AIへの送信は作らない。代わりに、手持ちのAIアプリで冷蔵庫を撮り、返ってきた一覧を貼り付ける手作業を3〜5回試す | DEC-007・G5。家の中・学校のプリント・会員番号が写らないように撮る |

## C. レビューで守る観点（`/home-review` の各担当が確認すること）

- 「その操作を無くせないか」を、画面上の操作ひとつずつについて問う（G1）
- 事実確認：指摘は `app.html` の実態と照合する。01_CURRENT_SPEC と食い違ったら、どちらが正しいかを明記する
