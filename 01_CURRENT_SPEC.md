# 01 CURRENT SPEC（家事アプリ・現在の仕様）

> **正本。今の正しい仕様だけを書く。履歴（以前はこうだった）は書かない。** 実装は `app.html`（1ファイル）。
> 最終更新：2026-09-25（schema_version 2 / 同意バージョン 2026-09-25）

## 1. 動かし方

- `app.html` をブラウザで開くだけ。サーバー・ビルド・ログインは不要。AIや外部APIは使わない（普通のルールだけで動く）。

## 2. 画面構成（1画面・上から順）

1. **初回の同意画面**（同意がない／同意バージョンが違うときに全面表示。設定からも開ける）
2. **ステップ1：入力**
   - ① 使いたい食材（**0件でもOK**）
   - ② 今日どうする？ ⚡最速／🍳普通／🎨料理したい（**選ばなくてもOK**。もう一度押すと解除）
   - 「献立候補を見る」
3. **ステップ2：候補（最大3案）**＋注意書き。「食材・モードを変える」で戻る
4. **ステップ3：きまり！（もう記録しました）** 開いてから決めるまでの秒数を表示。「OK」で入力に戻り、計測もリセット
5. **さいきんの記録**（最新10件）＋「記録を書き出す（JSON）」「全部消す」
6. **記録の見直し**（90日を過ぎた記録があるときだけ表示）
7. **設定**：外したい食材／同意の状態・「同意内容を見る・変える」「同意をやめて全部消す」

## 3. 入力

- 読点・カンマ・中黒・スペース・改行で区切って、まとめて入力・貼り付けできる。スマホの音声入力もそのまま使える。
- 「献立候補を見る」を押すと、入力欄に残った文字（Enterの押し忘れ）も食材として取り込む。
- 日本語変換を確定するEnter（`isComposing`／keyCode 229）では登録しない。
- **よく使う食材チップ**：過去の記録の上位8件（記録がないときは 鶏肉・豚肉・卵・キャベツ・玉ねぎ・豆腐・にんじん・もやし）。タップで追加。
- 入力した食材のタグには読み取り結果を表示：「豚こま→豚肉」、辞書にない語は「？」、除外中の食材は赤いタグ。

## 4. 食材の読み取り（normalizeIngredient）

1. 辞書（SYNONYMS）に完全一致 → 代表名
2. 入力が2文字以上なら、辞書の語を**長い順**に見て、**入力がその語を含めば**代表名（`partial`＝読み替え）。1文字の入力は完全一致のみ
3. どちらでもなければ入力のまま（辞書の代表名に一致すれば `known`、しなければ辞書外）

- 例：「豚こま」→豚肉、「玉ねぎ」→玉ねぎ（「ねぎ」にはならない）、「肉」「米粉」「ね」→辞書外（？）、「そば粉」→麺（読み替え）

## 5. 献立の候補

- レシピ42件をコードに持つ（id・料理名・主な食材・調理分数・向いているモード）。
- **除外食材を含むレシピは、点数を付ける前に候補から外す**（順位より優先）。
- スコア＝手持ち食材との一致数＋記録からの補正。
- **記録からの補正**（同じレシピの最新の記録を見る）：👍 +0.5／👎 −2／3日以内に作った（「作らなかった」以外）−1。補正の理由（「前回👍」「前回👎なので控えめ」「最近作ったので控えめ」）を候補に表示。
- 並び順：スコア → 買い足しの少なさ → 調理時間（🎨料理したいは長い順、ほかは短い順）。
- モードを選んだとき：そのモードのレシピから上位3件。モード未選択：⚡・🍳・🎨から1案ずつ（重複なし）。
- 除外で候補が3件に満たないときは、その旨を表示。

### 候補カード
- 料理名、「買い物不要」バッジ（買い足し0件のとき）、約○分・どのモード向けか
- ✓手持ちの食材／🛒買い足す食材
- 「作り方を見る ↗」＝Googleで「料理名 レシピ」を検索するリンク（作り方は自前で持たない）
- 注意書き（該当時）：除外中の食材一覧／アレルギー登録中は「材料表は主な食材だけ。調味料・加工品は作り方のページで必ず確認」／辞書外・読み替えた除外食材があるときは「候補から確実には外せません」

## 6. 記録と評価

- **候補を選んだ時点で自動保存**（`status:'planned'`）。同意がないときは保存せず同意画面を開く。
- 食後の評価は、記録の一番上に並ぶ「予定」の行で 👍😐👎／作らなかった を1タップ。**任意・催促なし。**
- 評価済みの行は、絵文字をタップすると 👍→😐→👎→作らなかった の順に切り替わる（押し間違いの修正用）。

## 7. データモデル

- 保存先：ブラウザのlocalStorage（外部には送らない）。キーは2つ。

### `household_meal_log`（記録の配列）
```
{ schema_version: 2, id, date, status: 'planned'|'rated'|'skipped',
  mode: 'quick'|'normal'|'fun'|'mixed'(未選択), recipe_mode,
  ingredients_input, ingredients_canon, unknown_ingredients,
  candidates_shown(レシピID), recipe_id, selected_recipe, recipe_ing, missing_ing,
  family_rating: 'good'|'mid'|'bad'|null, consent_version, rated_at?, keep_until?,
  // 計測
  open_to_decide_seconds, input_seconds, tap_count, input_routes: ['typed'|'paste'|'chip'], removed_chip_count,
  saved_at }
```

### `household_settings`
```
{ consent: { version, service: true, research: bool, external_ai: false, agreed_at } | null,
  exclusions: [{ raw, canon, known, partial, kind: 'allergy'|'dislike', added_at }] }
```
- 同じ食材がアレルギーと苦手の両方に登録されたら、アレルギーとして扱う。

### 書き出しJSON
`{ schema_version, exported_at, settings, meal_log }`（ファイル名 `household_meal_log_YYYY-MM-DD.json`）

## 8. 計測（「触る時間が短いほど成功」を測る）

| 項目 | 意味 |
|---|---|
| open_to_decide_seconds | 画面を開いてから（または前回の決定後から）決めるまでの秒数 |
| input_seconds | 食材を入力していた秒数 |
| tap_count | タップの回数 |
| input_routes | 入力の経路（手入力／貼り付け／チップ） |
| removed_chip_count | ×で消した食材タグの数 |

## 9. 同意・保存期間・削除

- 同意画面で表示すること：記録するもの／保存場所（この端末のブラウザの中だけ）／写真・音声は扱わない／保存期間／持ち出し・削除の方法。
- 同意の目的：**サービス利用（必須）**／**研究利用（任意）**。外部AI処理・連携は「今は使っていない。使うときは送る内容・保存期間・学習への利用・削除方法を確認してから別に同意」。
- 同意にはバージョン（`2026-09-25`）があり、変わると同意を取り直す。
- **保存期間**：90日を過ぎた記録は自動では消さず、「書き出して消す」または「残す（90日後にまた確認＝`keep_until`）」を聞く。
- 「全部消す」＝記録を全削除（確認あり）。「同意をやめて全部消す」＝記録と設定を全削除し、同意画面に戻る。
- 保存に失敗したら、画面下の通知（toast）で知らせる。

## 10. 安全・表示の実装

- 画面に出す文字列は `escapeHtml` を通す。食材チップはdata属性経由で渡す（入力文字をonclickに直接埋めない）。
